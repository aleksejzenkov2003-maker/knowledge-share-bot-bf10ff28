

## Правки карточки ТЗ и отображения МКТУ

### Изменения в `src/pages/Trademarks.tsx`

#### 1. Detail Dialog — убрать лишние заголовки, добавить воздух
- Секции уже без заголовков «Основная информация» и «Дополнительно» — ок
- Убрать слово «Публикация» у ссылки (оставить только иконку + текст «Публикация» → заменить на иконку ExternalLink + URL без подписи, или оставить иконку)
  - Нет, по скриншоту видно что ссылка «Публикация» дублирует — убрать текст «Публикация», оставить только ссылку-иконку
- Добавить `right_holder_address` в блок правообладателя (уже есть на строке 923 — ок)
- Увеличить `space-y-5` → `space-y-6` для больше воздуха между секциями
- Формат дат — уже DD.MM.YYYY через `fmtDate` — ок

#### 2. ФИПС диалог — убрать заголовки секций
- Убрать `<h4>Основная информация</h4>`, `<h4>Правообладатель</h4>`, `<h4>Характеристики</h4>` (строки 1096, 1112, 1122)
- Добавить `Separator` между секциями вместо заголовков
- Формат дат в ФИПС диалоге — данные приходят как `YYYY-MM-DD`, нужно тоже через `fmtDate`
- Добавить `right_holder_address` если есть в fipsData

#### 3. МКТУ — визуальное разделение классов
Текст `classes_mktu` содержит строки вида `42 – описание товаров...`. Нужно:
- Парсить текст: разбить по паттерну `\n?(\d+)\s*[–-]\s*` на отдельные классы
- Каждый класс рендерить в отдельном блоке с отступом `mb-3`
- Номер класса — **жирный + красный** (`font-bold text-red-600`)
- Применить в обоих местах (detail dialog и FIPS dialog)

#### 4. Создать вспомогательную функцию `renderMktu`
```typescript
function renderMktu(text: string) {
  // Разбить на классы по паттерну "число – "
  // Рендерить номер жирным красным, текст обычным
}
```

### Файлы
- **`src/pages/Trademarks.tsx`** — все изменения в одном файле

